extends ../layout/index

block contenido
    div(class="min-h-screen flex flex-col items-center justify-center bg-gray-100 py-12 px-4 sm:px-6 lg:px-8")
        div(class="max-w-md w-full space-y-8 bg-white rounded-2xl shadow-2xl p-10 text-center")
            div
                i(class="fas fa-exclamation-triangle text-6xl text-[#FF6819] mb-6 animate-bounce")
                h1(class="text-4xl font-extrabold text-gray-900 mb-2") 404
                h2(class="text-2xl font-semibold text-gray-700 mb-4") Página no encontrada
                p(class="text-gray-500 mb-6")
                    | Lo sentimos, la página que buscas no existe o fue movida.
            a(href="/auth/login" class="inline-block mt-4 px-6 py-3 bg-[#FF6819] text-white rounded-lg shadow-lg hover:bg-[#e55b16] transition-colors text-lg font-semibold")
                i(class="fas fa-sign-in-alt mr-2")
                | Ir a Iniciar Sesión
