div(id="modalMensaje" class="fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-50 hidden")
    div(class="bg-white rounded-2xl shadow-2xl max-w-sm w-full mx-4 transform transition-all duration-300 scale-95 opacity-0")
        //- Contenido del modal
        div(class="p-8 text-center")
            //- Icono
            div(class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-6" id="contenedorSpinner")
                //- Check
                div(class="flex items-center justify-center hidden" id="contenedorIconoCheck")
                    i(class="fas fa-check text-green-500 text-2xl" id="iconoCheck")
                
            //- Mensaje dinámico
            div(id="mensajeModal" class="mb-6")
                h3(class="text-xl font-semibold text-gray-900 mb-2")= mensajesFlash[0]
                p(id="mensajeCarga" class="text-sm text-gray-600") Procesando...
            
            //- Botones
            div(class="flex justify-center gap-4")
                button(
                    id="btnAceptar"
                    onclick="cerrarModalMensaje()"
                    disabled
                    class="px-6 py-2 bg-gray-300 text-gray-500 rounded-lg cursor-not-allowed transition-all duration-200 opacity-50"
                ) Aceptar