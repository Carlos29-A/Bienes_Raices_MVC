extends ../layout/index.pug

block contenido
    div(class="min-h-screen bg-gray-100 flex")
        include ../usuario/vendedor-Sidebar/Vendedor-Sidebar.pug

        //- Contenido Principal
        div(class="flex-1 flex flex-col shadow-lg")
            //- Encabezado
            header(class="bg-white border-b border-gray-200 px-8 py-6 shadow-lg")
                div(class="flex items-center justify-between")
                    div(class="flex items-center space-x-4")
                        div(class="bg-blue-100 p-3 rounded-full")
                            i(class="fas fa-envelope text-2xl text-blue-600")
                        div
                            h1(class="text-2xl font-bold text-gray-900") Mensajes de #{destinatario.nombre}

            //- Contenedor de Mensajes
            div(class="flex-1 p-8 bg-gray-50")
                //- Lista de Mensajes
                div(class="max-w-4xl mx-auto space-y-6")
                    each mensaje in mensajes
                        //- Contenedor individual de mensaje
                        div(class="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition-shadow duration-300")
                            //- Encabezado del mensaje
                            div(class="p-6")
                                div(class="flex items-start justify-between")
                                    //- Información del remitente y propiedad
                                    div(class="flex items-start space-x-4")
                                        img(
                                            src="/uploads/perfil.jpg"
                                            class="w-12 h-12 rounded-full object-cover border-2 border-blue-100"
                                            alt=mensaje.remitenteRelacion.nombre
                                        )
                                        div(class="space-y-2")
                                            //- Datos del comprador
                                            div
                                                h3(class="text-lg font-semibold text-gray-900")= mensaje.remitenteRelacion.nombre
                                                p(class="text-sm text-gray-600")= mensaje.remitenteRelacion.email
                                            //- Datos de la propiedad
                                            div
                                                h4(class="text-sm font-semibold text-gray-900")= mensaje.propiedadRelacion.titulo
                                                p(class="text-sm text-blue-600 font-bold")= `$ ${mensaje.propiedadRelacion.precio.toLocaleString()}`
                                    

                            //- Vista previa del mensaje
                            div(class="px-6 py-4 bg-gray-50 border-t border-gray-100")
                                p(class="text-gray-700 line-clamp-1")= mensaje.mensaje + '...'

                            //- Botones de acción
                            div(class="px-6 py-4 bg-gray-50 border-t border-gray-100 flex justify-end space-x-3")
                                a(href=`/mensajes/ver/${mensaje.id}` class="inline-flex items-center px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-md hover:bg-blue-700 hover:scale-105 transition-all duration-300")
                                    i(class="fas fa-eye mr-2")
                                    | Ver Mensaje
                                a(href=`/mensajes/responder/${mensaje.id}` class="inline-flex items-center px-4 py-2 bg-green-600 text-white text-sm font-medium rounded-md hover:bg-green-700 hover:scale-105 transition-all duration-300")
                                    i(class="fas fa-reply mr-2")
                                    | Responder

                //- Mensaje Informativo
                div(class="max-w-4xl mx-auto mt-8 bg-blue-50 rounded-lg p-6")
                    div(class="flex items-start space-x-4")
                        div(class="flex-shrink-0")
                            i(class="fas fa-info-circle text-2xl text-blue-600")
                        div
                            h4(class="text-lg font-semibold text-blue-900 mb-2") Información Importante
                            p(class="text-blue-700")
                                | Para mantener una comunicación efectiva, te recomendamos:
                            ul(class="list-disc list-inside mt-2 space-y-1 text-blue-700")
                                li Ser claro y conciso en tus respuestas
                                li Mantener un tono profesional y cordial

block scripts
    